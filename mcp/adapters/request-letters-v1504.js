// spec-v1504: MCP adapters for the request and appeal letter builders. The dom keys mirror views/group-v1504.js.

import * as RL from '../../lib/request-letters-v1504.js';

const vals = (xs) => xs.map((x) => x.value);
const s = (dom, arg, label, required = false) => ({ dom, arg, kind: 'string', required, label });

export default [
  {
    id: 'partd-redetermination-request',
    summary: 'Builds a Part D redetermination request with its deadline. Cites 42 CFR 423.580-423.590 and adds the expedited or good-cause wording the rule recognizes.',
    compute: RL.partdRedeterminationRequest,
    fields: [
      s('prr-notice', 'noticeDate', 'Coverage determination notice date (YYYY-MM-DD)', true),
      s('prr-received', 'receivedDate', 'Date the notice was received, if later than presumed (YYYY-MM-DD)'),
      s('prr-enrollee', 'enrollee', 'Enrollee name'),
      s('prr-member', 'memberId', 'Member ID'),
      s('prr-plan', 'plan', 'Plan name'),
      s('prr-drug', 'drug', 'Drug, strength and quantity'),
      s('prr-requester', 'requester', 'Name of enrollee or prescriber signing'),
      { dom: 'prr-exp', arg: 'expedited', kind: 'enum', required: false, values: vals(RL.YES_NO), label: 'Ask for an expedited redetermination' },
      { dom: 'prr-prescriber', arg: 'prescriberStatement', kind: 'enum', required: false, values: vals(RL.YES_NO), label: 'The prescriber makes the expedited statement' },
      { dom: 'prr-good', arg: 'goodCause', kind: 'enum', required: false, values: vals(RL.YES_NO), label: 'Asking for a good-cause extension (filed late)' },
      s('prr-date', 'requestDate', 'Request date (YYYY-MM-DD; blank for today)'),
    ],
  },
  {
    id: 'erisa-appeal-letter',
    summary: 'Builds an employer-plan appeal that asserts the claimant\'s ERISA review rights. Cites 29 CFR 2560.503-1(h) and states the decision deadline.',
    compute: RL.erisaAppealLetter,
    fields: [
      s('eal-denial', 'denialReceived', 'Date the denial was received (YYYY-MM-DD)', true),
      { dom: 'eal-kind', arg: 'claimKind', kind: 'enum', required: true, values: vals(RL.CLAIM_KINDS), label: 'Kind of claim' },
      s('eal-claimant', 'claimant', 'Claimant name'),
      s('eal-member', 'memberId', 'Member ID'),
      s('eal-claim', 'claimId', 'Claim or reference number'),
      s('eal-plan', 'plan', 'Plan name'),
      s('eal-service', 'service', 'Service, drug or item denied'),
      s('eal-date', 'requestDate', 'Letter date (YYYY-MM-DD; blank for today)'),
    ],
  },
  {
    id: 'ma-reconsideration-request',
    summary: 'Builds a Medicare Advantage reconsideration request with its deadline. Cites 42 CFR 422.578-422.590, with the expedited or good-cause wording the rule recognizes.',
    compute: RL.maReconsiderationRequest,
    fields: [
      s('mar-notice', 'noticeDate', 'Organization determination notice date (YYYY-MM-DD)', true),
      { dom: 'mar-item', arg: 'item', kind: 'enum', required: true, values: vals(RL.MA_ITEMS), label: 'What was denied' },
      s('mar-received', 'receivedDate', 'Date the notice was received, if later than presumed (YYYY-MM-DD)'),
      s('mar-enrollee', 'enrollee', 'Enrollee name'),
      s('mar-member', 'memberId', 'Member ID'),
      s('mar-plan', 'plan', 'Plan name'),
      s('mar-service', 'service', 'Service, item or drug denied'),
      s('mar-requester', 'requester', 'Name of enrollee or physician signing'),
      { dom: 'mar-exp', arg: 'expedited', kind: 'enum', required: false, values: vals(RL.YES_NO), label: 'Ask for an expedited reconsideration' },
      { dom: 'mar-phys', arg: 'physicianStatement', kind: 'enum', required: false, values: vals(RL.YES_NO), label: 'The physician makes the expedited statement' },
      { dom: 'mar-good', arg: 'goodCause', kind: 'enum', required: false, values: vals(RL.YES_NO), label: 'Asking for a good-cause extension (filed late)' },
      s('mar-date', 'requestDate', 'Request date (YYYY-MM-DD; blank for today)'),
    ],
  },
  {
    id: 'external-review-request',
    summary: 'Builds an external review request with its four-month deadline. Cites 45 CFR 147.136 and adds the expedited or deemed-exhaustion paragraph when marked.',
    compute: RL.externalReviewRequest,
    fields: [
      s('err-denial', 'denialReceived', 'Date the final internal denial was received (YYYY-MM-DD)', true),
      s('err-claimant', 'claimant', 'Claimant name'),
      s('err-member', 'memberId', 'Member ID'),
      s('err-plan', 'plan', 'Plan or issuer name'),
      s('err-service', 'service', 'Service, drug or item denied'),
      { dom: 'err-exp', arg: 'expedited', kind: 'enum', required: false, values: vals(RL.YES_NO), label: 'Ask for an expedited external review' },
      { dom: 'err-deemed', arg: 'deemedExhaustion', kind: 'enum', required: false, values: vals(RL.YES_NO), label: 'The plan did not follow the internal-appeal rules (deemed exhaustion)' },
      s('err-date', 'requestDate', 'Request date (YYYY-MM-DD; blank for today)'),
    ],
  },
  {
    id: 'medicaid-hearing-request',
    summary: 'Builds a Medicaid managed care appeal or fair hearing request with its deadline. Adds the continued-benefits request and its 10-day warning when marked.',
    compute: RL.medicaidHearingRequest,
    fields: [
      { dom: 'mhr-kind', arg: 'kind', kind: 'enum', required: true, values: vals(RL.MEDICAID_KINDS), label: 'Plan appeal or fair hearing' },
      s('mhr-notice', 'noticeDate', 'Date on the notice (YYYY-MM-DD)', true),
      { dom: 'mhr-days', arg: 'stateDays', kind: 'number', required: false, label: 'The state fair hearing window in days (fee-for-service; at most 90)' },
      s('mhr-enrollee', 'enrollee', 'Enrollee name'),
      s('mhr-member', 'memberId', 'Medicaid ID'),
      s('mhr-plan', 'plan', 'Plan or state agency name'),
      s('mhr-service', 'service', 'Service or benefit'),
      { dom: 'mhr-keep', arg: 'keepBenefits', kind: 'enum', required: false, values: vals(RL.YES_NO), label: 'Ask to keep benefits during the appeal' },
      { dom: 'mhr-exp', arg: 'expedited', kind: 'enum', required: false, values: vals(RL.YES_NO), label: 'Ask for an expedited plan appeal' },
      s('mhr-date', 'requestDate', 'Request date (YYYY-MM-DD; blank for today)'),
    ],
  },
  {
    id: 'partd-exception-request',
    summary: 'Builds a Part D exception request with the prescriber statement the rule requires. States the tiering, formulary, step therapy or dose standard of 42 CFR 423.578.',
    compute: RL.partdExceptionRequest,
    fields: [
      { dom: 'per-type', arg: 'type', kind: 'enum', required: true, values: vals(RL.EXCEPTION_TYPES), label: 'Kind of exception' },
      { dom: 'per-basis', arg: 'basis', kind: 'enum', required: false, values: vals(RL.BASES), label: 'Basis the prescriber states (not needed for a dose restriction)' },
      { dom: 'per-statement', arg: 'statement', kind: 'enum', required: false, values: vals(RL.YES_NO), label: 'The prescriber provides the supporting statement' },
      s('per-drug', 'drug', 'Drug, strength and quantity'),
      s('per-enrollee', 'enrollee', 'Enrollee name'),
      s('per-member', 'memberId', 'Member ID'),
      s('per-plan', 'plan', 'Plan name'),
      s('per-prescriber', 'prescriber', 'Prescriber name and NPI'),
      s('per-requester', 'requester', 'Name of enrollee or prescriber signing'),
      { dom: 'per-exp', arg: 'expedited', kind: 'enum', required: false, values: vals(RL.YES_NO), label: 'Ask for an expedited decision' },
      s('per-date', 'requestDate', 'Request date (YYYY-MM-DD; blank for today)'),
    ],
  },
  {
    id: 'medical-necessity-letter',
    summary: 'Builds a letter of medical necessity for a drug. Lays out the diagnosis, prior therapies and payer criteria, leaving the clinical rationale to the prescriber.',
    compute: RL.medicalNecessityLetter,
    fields: [
      s('mnl-drug', 'drug', 'Drug, dose and schedule', true),
      s('mnl-dx', 'diagnosis', 'Diagnosis with ICD-10-CM code', true),
      s('mnl-dxdate', 'diagnosisDate', 'Date of diagnosis'),
      { dom: 'mnl-label', arg: 'labeled', kind: 'enum', required: false, values: vals(RL.YES_NO), label: 'The use is an FDA-approved indication' },
      s('mnl-patient', 'patient', 'Patient name'),
      s('mnl-member', 'memberId', 'Member ID'),
      s('mnl-plan', 'plan', 'Plan name'),
      s('mnl-prior', 'priorTherapies', 'Prior therapies, one per line'),
      s('mnl-criteria', 'criteria', 'Payer criteria and where the evidence is, one per line'),
      s('mnl-prescriber', 'prescriber', 'Prescriber name, credentials and NPI'),
      s('mnl-date', 'letterDate', 'Letter date (YYYY-MM-DD; blank for today)'),
    ],
  },
];
